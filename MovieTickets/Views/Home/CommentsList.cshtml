@model IEnumerable<MovieTickets.Models.MovieComment>
<p>
    <div>Комментарии: </div>
    @foreach (var c in Model)
    {
        @Html.DisplayFor(comment => c.Title)
        <br />
        @Html.DisplayFor(comment => c.Text)
    }
</p>





